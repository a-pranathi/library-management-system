Library Management System
Pranathi Ayyadevara
22f3001788

To create the virtual environment, go to command line and type
python -m venv .venv (.venv being the name of the virtual environment)
To install the necessary packages, execute
pip install -r requirements.txt
or execute each line from the installations.txt folder.

To run the app, type
python app.py